<%-  include("partials/header") -%>
    
    <% articles.forEach(article => { %>
        <h1>
            <%=article.title%>
        </h1>
        <p>
            <%= _.truncate(article.content, {"length": 100, "omission": '...'}) %>
            <a href="/articles/<%=article._id%>">Read More</a>
        </p>
    <% }); %> 
<%- include("partials/footer") -%> 